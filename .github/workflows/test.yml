name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest  # 指定使用的公共运行器
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Run a script
        run: echo "Hello, World!"
